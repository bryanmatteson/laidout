# Consumer rendering contract

This design is superseded by
[`prepared-layout-kernel-plan.md`](prepared-layout-kernel-plan.md), which is the
single live authority for the prepared consumer kernel, workspace modes,
standalone boundary, cost domain, and downstream console integration.

The 0.1 generic engines and output rope remain available under the optional
`research` feature; import replacements are listed in
[`migration-0.2.md`](migration-0.2.md).
